<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Room View</title>
</head>
<body>
<h1>Room View</h1>

<p><strong>Format:</strong> <%= format %></p>

<% if (room.code) { %>
    <p><strong>Room Code:</strong> <%= room.code %></p>
<% } %>

<div>
    <h2>Player</h2>
    <p>Username: <%= player.username %></p>
</div>

<div>
    <h2>Opponent</h2>
    <% if (opponent) { %>
        <p>Username: <%= opponent.username %></p>
    <% } else { %>
        <p><em>Waiting for opponent...</em></p>
    <% } %>
</div>
</body>
</html>
